# legal-timeline

A Python tool that extracts, categorises, and visualises key dates from legal Word documents (contracts, shareholder agreements, NDAs, etc.).

## Running

```bash
git clone 
cd legal_timeline
poetry install
poetry run visualise your_document.docx
```

